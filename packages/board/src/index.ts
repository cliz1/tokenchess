// Empty scaffold — Phase 2 moves in the chessground layer behind a
// message-passing contract. Must carry both Spike A requirements: sizing the
// board to a multiple of 8 CSS px, and owning bounds invalidation via
// ResizeObserver + visualViewport (chessground only watches document
// scroll / window resize, neither of which fires inside a WebView).
export {};
